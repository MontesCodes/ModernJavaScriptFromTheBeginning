'use strict';
const firstName = 'William';
const lastName = 'Johnson';

let val;

val = firstName + lastName;

// Concatenation

val = firstName + ' ' + lastName;

// Append
val = 'Montes ';
val += 'Codes';

console.log(val);
